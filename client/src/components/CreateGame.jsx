import React, { useState } from 'react';
import socket from '../socketConfig.js';

const CreateGame = () => {
  // const [] = useState('');

  return (
    <div>
      <h1>Create Game</h1>
    </div>
  );
};